'use strict'

//pedir dos numeros y compararlos/* 
/*  */
/* var a;
var b;

a = prompt('intruduce un numero');
b = prompt('introduce otro numero');

while (a<=0 || b<=0  || isNaN(a) || isNaN(b)){ isNaN es para ver si es un numero  
    a = prompt('intruduce un numero mayo a 0 o que no sea letra');
b = prompt('introduce un numero ');

}
if (a == b){
    console.log('son iguales');
}
else if (a<b){
    console.log(`el nummero ${a} es menor que ${b}`);
}
else{
    console.log('el numero '+b+' es menor que '+a);
}
 */

 var a=0;
 var b=0;

 do{
    
 }while( a < 0)